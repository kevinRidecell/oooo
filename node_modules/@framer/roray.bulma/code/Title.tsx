import * as React from 'react';
import { PropertyControls, ControlType } from 'framer';
import { Title as _Title } from 'bloomer';

type HeadingSizes = 1 | 2 | 3 | 4 | 5 | 6;

// Define type of property
interface Props {
  text: string;
  isSize: HeadingSizes;
  isSpaced: boolean;
}

export class Title extends React.Component<Props> {
  // Set default properties
  static defaultProps = { text: 'Title' };

  // Items shown in property panel
  static propertyControls: PropertyControls = {
    text: { type: ControlType.String, title: 'Text' },
    isSize: {
      title: 'Size',
      type: ControlType.Number,
      min: 1,
      max: 6,
      step: 1
    },

    isSpaced: { title: 'Spaced', type: ControlType.Boolean }
  };

  render() {
    return <_Title {...this.props}>{this.props.text}</_Title>;
  }
}
